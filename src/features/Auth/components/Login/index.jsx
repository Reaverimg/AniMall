import React from "react";
import PropTypes from "prop-types";
import LoginForm from "../../forms/LoginForm";

Login.propTypes = {};

function Login(props) {
  return <div>{/* <LoginForm onSubmit={}></LoginForm> */}</div>;
}

export default Login;
