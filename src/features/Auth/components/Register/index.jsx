import React from "react";
import PropTypes from "prop-types";
import RegisterForm from "../../forms/RegisterForm";

Register.propTypes = {};

function Register(props) {
  return <div>{/* <RegisterForm onSubmit={}></RegisterForm> */}</div>;
}

export default Register;
